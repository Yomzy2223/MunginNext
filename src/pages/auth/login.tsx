import AuthLayout from "@/layout/AuthLayout";
import LoginNew from "@/PagesComponents/AuthNew/login";
import React from "react";

const Login = () => {
  return (
    <AuthLayout>
      <LoginNew />
    </AuthLayout>
  );
};

export default Login;
